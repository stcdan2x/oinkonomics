// Fallback while a lazily loaded route or chart module arrives (step 10.0).
export default function Loading() {
  return <div className="px-4 py-8 text-center text-sm text-slate-400">Loading...</div>
}
